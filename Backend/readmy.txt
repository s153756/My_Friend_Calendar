
Create and starst image

1. docker build -t <name_image:version> . # create image
2. docker run --name <name_container> -d -p 5000:5000 <name_image:version> # create and start container on host 5000


project/
│
├── app/                          # Główny pakiet aplikacji Flask 
│   │  ... (szczegóły poniżej)
│
├── instance/                     # Konfiguracja specyficzna
│   └── config.py
│
├── migrations/                   # Skrypty do zarządzania zmianami w schemacie bazy danych.
│
├── tests/                        # Testy jednostkowe/integracyjne
│   ├── conftest.py
│   ├── test_users.py
│   └── test_events.py
│
├── .env                          # Zmienne środowiskowe
├── .dockerignore                 # git ignor tylko dla dockera
├── docker-compose.yml            # Definiuje i łączy kontener
├── Dockerfile                    # Instrukcje budowania obrazu flask
├── requirements.txt              # Zależności Pythona
└── run.py                        # Uruchamia aplikację flask

app/
├── __init__.py               # Tworzy i konfiguruje aplikację
├── config.py                 # Konfiguracja domyślna
├── extensions.py             # inicjalizacji rozszerzeń
│
├── routes/                  
│   ├── __init__.py           # Rejestruje Blueprinty
│   ├── user_routes.py        # Logika tras dla Użytkowników
│   └── calendar_routes.py    # Logika tras dla Kalendarza
│
├── models/                   # WARSTWA MODELI
│   ├── __init__.py
│   ├── user.py               # Definicja modelu bazy danych User
│   └── event.py              # Definicja modelu bazy danych Event
│
├── services/                 # WARSTWA SERWISÓW 
│   ├── __init__.py
│   ├── user_service.py       # Logika biznesowa związana z User
│   └── calendar_service.py   # Logika biznesowa Kalendarza
│
├── schemas/                  # WARSTWA SERIALIZACJI/WALIDACJI
│   ├── __init__.py
│   ├── user_schema.py        # Schematy dla danych User
│   └── event_schema.py       # Schematy dla danych Event
│
└── utils/                    # NARZĘDZIA POMOCNICZE
    ├── __init__.py
    └── auth.py               # Funkcje związane z uwierzytelnianiem (np. dekoratory JWT, pomoc w haszowaniu)